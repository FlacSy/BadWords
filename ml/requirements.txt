# Training pipeline only. Inference needs none of this - see the `ml` extra of
# badwords-py (onnxruntime + transformers + numpy), which never imports torch.
# For GPU: pip install torch --index-url https://download.pytorch.org/whl/cu124
datasets>=2.14.0
transformers>=4.36.0
accelerate>=0.26.0
torch>=2.13.0
optimum[onnxruntime]>=1.14.0
pandas>=2.0.0
scikit-learn>=1.3.0
onnx>=1.15.0
